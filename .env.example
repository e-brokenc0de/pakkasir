# Copy to `.env` and fill in — NEVER commit the real file.
PAKASIR_API_KEY=
PAKASIR_PROJECT=
# Optional — override the base URL (e.g. for a self-hosted sandbox).
# PAKASIR_BASE_URL=https://app.pakasir.com
